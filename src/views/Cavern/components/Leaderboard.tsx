import React, { useState } from 'react';

const CavernLeaderBoard = () => {
  const [holder, setHolder] = useState(null);

  return <div>Leaderboard</div>;
};

export default CavernLeaderBoard;
